<template>
  <div class="mobile-container">
    <header class="header">
      <div class="top-bar">
        <div class="search-icon">🔍</div>
        <div class="capsule-menu">
          <span>•••</span> | <span>—</span> | <span>◎</span>
        </div>
      </div>
      
      <div class="store-info">
        <div class="store-details">
          <h2 class="store-name">北京市昌平区鼓楼西大街店 <span class="arrow">›</span></h2>
          <p class="distance">📍 距离您490m</p>
          <div class="promo-tag"><span>特价</span> 特价商品9.9元起 <span class="link">详情 ></span></div>
        </div>
        <button class="pickup-btn">到店取餐</button>
      </div>

      <div class="header-banner-placeholder">
        <div class="banner-content">全家福两件套/三件套 (广告图占位)</div>
      </div>
    </header>

    <div class="toggle-tabs">
      <div class="tab">熟食</div>
      <div class="tab active">生鲜自提</div>
    </div>

    <main class="main-content">
      <aside class="sidebar">
        <div 
          v-for="(cat, index) in categories" 
          :key="index"
          class="cat-item"
          :class="{ active: activeCategory === index }"
          @click="activeCategory = index"
        >
          <span v-if="cat.icon" class="cat-icon">{{ cat.icon }}</span>
          {{ cat.name }}
        </div>
      </aside>

      <section class="product-list">
        <h3 class="section-title">{{ categories[activeCategory].name }}</h3>
        
        <div class="inner-banner">
          <div class="img-box red-bg">生鲜现包 买回家煮 (广告图)</div>
        </div>

        <div class="product-card">
          <div class="product-img-box">
             <span>商品图片</span>
             <div class="overlay-tag">¥9.9 购卡领券 ></div>
          </div>
          <div class="product-info">
            <h4>鲜虾蟹籽云吞</h4>
            <div class="price-row">
              <span class="price">¥22</span>
              <button class="add-btn">+</button>
            </div>
          </div>
        </div>

        <div class="product-card" v-for="i in 3" :key="i">
          <div class="product-img-box">
             <span>商品图片 {{ i }}</span>
          </div>
          <div class="product-info">
            <h4>玉米鲜肉饺子</h4>
            <div class="price-row">
              <span class="price">¥18</span>
              <button class="add-btn">+</button>
            </div>
          </div>
        </div>
      </section>
    </main>

    <footer class="footer-cart">
      <div class="cart-icon-wrapper">
        <div class="cart-icon">🛒</div>
      </div>
      <div class="cart-info">
        <span class="empty-text">未选购商品</span>
      </div>
      <button class="checkout-btn">去结算</button>
    </footer>
  </div>
</template>

<script setup>
import { ref } from 'vue';

// 模拟分类数据
const categories = ref([
  { name: '生食饺子', icon: '🥟' },
  { name: '生食云吞', icon: '' },
  { name: '劲道面食', icon: '' },
  { name: '食饺蘸料', icon: '' },
  { name: '下单贴士', icon: '' },
  { name: '冷冻速食', icon: '' },
]);

const activeCategory = ref(0);
</script>

<style scoped>
/* 基础重置 */
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

.mobile-container {
  width: 100%;
  max-width: 450px; /* 模拟手机宽度 */
  height: 100vh;
  margin: 0 auto;
  background-color: #fff;
  display: flex;
  flex-direction: column;
  font-family: -apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", sans-serif;
  color: #333;
  border: 1px solid #eee;
}

/* --- Header 样式 --- */
.header {
  padding: 10px 12px 0;
  background: #fff;
}

.top-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 10px;
}

.capsule-menu {
  border: 1px solid #ddd;
  border-radius: 12px;
  padding: 2px 8px;
  font-size: 12px;
  background: rgba(255,255,255,0.8);
}

.store-info {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
}

.store-name {
  font-size: 16px;
  font-weight: bold;
}

.distance {
  font-size: 12px;
  color: #666;
  margin: 4px 0;
}

.promo-tag {
  font-size: 11px;
  color: #ff4d4f;
  display: flex;
  align-items: center;
}
.promo-tag span:first-child {
  border: 1px solid #ff4d4f;
  padding: 0 2px;
  margin-right: 4px;
  border-radius: 2px;
}
.promo-tag .link {
  margin-left: auto;
  color: #999;
}

.pickup-btn {
  background-color: #4d927c;
  color: #fff;
  border: none;
  padding: 4px 10px;
  border-radius: 15px;
  font-size: 12px;
}

.header-banner-placeholder {
  margin-top: 10px;
  background: #fff5e0;
  border-radius: 8px;
  height: 80px; /* 顶部大Banner高度 */
  display: flex;
  align-items: center;
  justify-content: center;
  color: #b58c42;
  font-size: 14px;
  overflow: hidden;
}

/* --- Toggle Tabs 样式 --- */
.toggle-tabs {
  display: flex;
  padding: 10px 12px;
  gap: 10px;
}

.tab {
  flex: 1;
  text-align: center;
  padding: 8px 0;
  background: #f5f5f5;
  border-radius: 4px;
  font-size: 14px;
  font-weight: 500;
  color: #666;
}

.tab.active {
  background: #4d927c; /* 选中的绿色 */
  color: white;
}

/* --- Main Content (左右布局) --- */
.main-content {
  flex: 1; /* 占据剩余高度 */
  display: flex;
  overflow: hidden; /* 防止外层滚动 */
  border-top: 1px solid #f0f0f0;
}

/* 左侧侧边栏 */
.sidebar {
  width: 85px;
  background-color: #f7f8fa;
  overflow-y: auto;
}

.cat-item {
  padding: 15px 10px;
  font-size: 13px;
  color: #666;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
}

.cat-item.active {
  background-color: #fff;
  font-weight: bold;
  color: #333;
  border-left: 3px solid #4d927c; /* 选中时的左侧绿条 */
}

.cat-icon {
  font-size: 20px;
  margin-bottom: 4px;
}

/* 右侧商品列表 */
.product-list {
  flex: 1;
  background-color: #fff;
  overflow-y: auto;
  padding: 10px;
}

.section-title {
  font-size: 13px;
  color: #333;
  margin-bottom: 10px;
}

.inner-banner {
  height: 80px;
  background-color: #e53935;
  border-radius: 6px;
  margin-bottom: 15px;
  color: #fff;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: bold;
}

.product-card {
  margin-bottom: 20px;
}

.product-img-box {
  width: 100%;
  height: 140px;
  background-color: #ddd;
  border-radius: 6px;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  color: #888;
}

.overlay-tag {
  position: absolute;
  bottom: 10px;
  right: 10px;
  background: rgba(0,0,0,0.7);
  color: #d4a770;
  font-size: 10px;
  padding: 4px 8px;
  border-radius: 12px;
  border: 1px solid #d4a770;
}

.product-info h4 {
  margin-top: 8px;
  font-size: 15px;
}

.price-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: 5px;
}

.price {
  color: #e53935;
  font-weight: bold;
  font-size: 16px;
}

.add-btn {
  width: 24px;
  height: 24px;
  background: #4d927c;
  color: #fff;
  border: none;
  border-radius: 50%;
  font-size: 18px;
  line-height: 22px;
  cursor: pointer;
}

/* --- Footer 购物车 --- */
.footer-cart {
  height: 50px;
  background: #fff;
  border-top: 1px solid #eee;
  display: flex;
  align-items: center;
  padding: 0 15px;
  position: relative;
  z-index: 10;
}

.cart-icon-wrapper {
  position: relative;
  top: -10px; /* 购物车图标凸起 */
  background: #fff;
  border-radius: 50%;
  padding: 4px;
}

.cart-icon {
  width: 40px;
  height: 40px;
  background: #8e6a52; /* 棕色购物车 */
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
  font-size: 20px;
}

.cart-info {
  flex: 1;
  margin-left: 10px;
}

.empty-text {
  color: #999;
  font-size: 14px;
}

.checkout-btn {
  background-color: #9ccbc0; /* 未激活状态的浅绿色 */
  color: #fff;
  border: none;
  padding: 8px 25px;
  border-radius: 20px;
  font-weight: bold;
}
</style>